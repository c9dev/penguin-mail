/target
*.db
*.db-wal
*.db-shm
.claude/worktrees/
# Design-tool settings, which belong to whoever runs the tool.
.impeccable/
# graphify's knowledge graph of the repository, rebuilt on demand.
graphify-out/

# The owner's own working notes and agent setup stay on their machine.
.agents/
.claude/
skills-lock.json
docs/agents/
docs/superpowers/
docs/remaining-work.md

# The owner's build values for the Google and Microsoft clients.
packaging/secrets.env
__pycache__/
